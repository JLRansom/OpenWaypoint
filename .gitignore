# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files — never commit secrets
.env
.env.local
.env*.local

# debug
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts
.idea

# SQLite database and uploaded files
# data/uploads/ is created at runtime by the file attachment feature
/data/

# Git worktrees
.worktrees/

# Local agent skills — kept on disk but never committed
.agents/
